// Initial schema (version 1) is created via Drift's onCreate.
// Add migration logic here when bumping schemaVersion.
//
// Example for future migrations:
// if (from < 2) {
//   await m.addColumn(products, products.newColumn);
// }
